[package]
name = "semantics"
version = "0.1.0"
edition = "2024"

[dependencies]
shared_components = { path = "../../shared_components" }
gppvm_core = { path = "../../gppvm_core" }

parser = { path = "../parser" }
