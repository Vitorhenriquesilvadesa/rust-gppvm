type Vec3 {
    x: float,
    y: float,
    z: float,
}

type Ray {
    origin: Vec3,
    direction: Vec3,
}

def dot(a: Vec3, b: Vec3) -> float {
    let myList = 'Hello'.length;

    return a.x * b.x + a.y * b.y + a.z * a.z;
}

def main() -> int {
    let a;
    let b = Vec3(0.0, 1.0, 0.0);

    a = Vec3(0.0, 0.0, 1.0);

}