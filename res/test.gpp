def some_function(myList: number, someArg: iterator) -> bool {

    let a = [1, 2, 3];
    let b = [1, 2, 3];
    let y = [a, b];
    let z = [1.0, 2, true, 'hello'];

    if 'str' == 'str' {

    }
}

type Point {
}

def main() -> int {
    some_function(2.0, [1, 2, 3]);
}